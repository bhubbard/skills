---
name: gutenberg-custom-block
description: Developing custom Gutenberg blocks using the Block API.
---

# Gutenberg Custom Block
Guidelines for building custom WordPress blocks:
- Use `block.json` for metadata.
- Utilize `@wordpress/blocks` and React for `edit` and `save` functions.
